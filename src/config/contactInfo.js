/**
 * Site-wide contact details.
 *
 * This is the ONLY file you need to edit to update the dispatch phone
 * number or email address - it's used by both the Footer and the
 * Contact page, so it stays consistent everywhere automatically.
 */
export const CONTACT_INFO = {
  phone: '(800) 555-1234',
  phoneHref: 'tel:+18005551234',
  email: 'dispatch@mmdlogistics.com',
  emailHref: 'mailto:dispatch@mmdlogistics.com',
}
